# AI 行为准则（AGENTS.md）

> 本文件优先级最高。如与通用习惯或 AI 默认行为冲突，以本文件为准。

---

## 1. 核心元规则（必选）

- **沟通语言**：全程使用中文对话。
- **代码注释与 Docstring**：使用中文（便于理解）。
- **提交信息 (Commit)**：使用中文，格式为 `<类型>: <简短描述>`（如 `feat: 增加待办折叠功能`）。
- **面对报错**：先分析根因，再给出修改方案，不要直接抛出一整段代码替换。

---

## 2. 项目速览与文档地图

**一句话**：Windows 桌面「日历 + 待办」二合一记录工具。Tauri 2 + React + TypeScript + SQLite，单 exe 免安装，数据全本地，默认离线。

| 文档 | 角色 | 新对话中的用法 |
| :--- | :--- | :--- |
| `AGENTS.md`（本文件） | AI 宪法 | 必须全文加载 |
| `PROJECT_MAP.md` | 代码地图 | 快速浏览，知道文件在哪、干什么 |
| `TODO.md` | 进度便签 | 看当前做到哪、下一步是什么 |
| `doc/PRD.md` | 需求唯一权威 | 按需查阅相关章节，不要全文灌入 |
| `doc/技术方案.md` | 实现唯一权威 | 按需查阅；含数据字典与关键决策记录 |
| `doc/测试用例.md` | 防回归契约 | 改哪个模块先对照哪节用例；完成前跑回归最小集 |
| `doc/开发批次计划.md` | 开发执行计划 | 按批次开发与验收；批次进度看 TODO，范围/验收看本文件 |
| `doc/原型.html` | UI 契约 | 做 UI 任务时用浏览器打开对照；不要灌进对话 |
| `CHANGELOG.md` | 版本历史 | 仅发版/记历史时写 |

**分工铁律**：业务规则的唯一权威是 `doc/PRD.md`（核心规则在第 5 章）。本文件、代码注释、其他文档一律**只引用不复制**规则全文，避免多处维护打架。

---

## 3. 硬性红线（违反 = 返工）

1. **离线原则**：一期禁止引入任何网络依赖 crate / 网络请求代码；二期仅「发送测试邮件」「计划内提醒到点发送」两条显式路径允许联网，且必须 TLS。其余任何时刻零网络行为。
2. **技术栈锁定**：不引入 UI 组件库、日历库、重型依赖；新依赖必须在对话中说明理由并经用户同意。
3. **数据收口**：所有 SQL 只能存在于 `src-tauri` 的 `store` 模块；前端不直接接触 SQL，一律走 IPC command。
4. **可撤销**：所有用户数据的写操作（事项/分类/字段/导入）必须经 undo 命令包装，保证 Ctrl+Z 可回退。
5. **敏感信息**：SMTP 授权码必须 DPAPI 加密入库；禁止明文落盘、禁止写入日志、禁止写入备份文件。
6. **数据目录**：运行期读写仅限 exe 同目录 `data\`；禁止写用户主目录、注册表、系统目录。
7. **schema 演进**：数据库结构变更必须新增迁移脚本（`schema_migrations` 管理），禁止修改历史迁移。

---

## 4. 代码风格

### TypeScript（`src/` 前端）
- 使用 `const`/`let`，禁止 `var`；优先箭头函数。
- 展示组件保持无状态（pure）；横条布局、日期计算等逻辑写成**纯函数**并配 vitest 单测。
- 与 Rust 结构体对应的类型集中放在 `services/`，两侧行为必须对齐；禁止 `any`。

### Rust（`src-tauri/` 后端）
- 遵循 `rustfmt`，`clippy` 零警告。
- 所有 `Result` 显式处理；运行路径禁止 `unwrap()`/`expect()`（程序初始化阶段除外）。
- 单文件不超过 500 行，超出按职责拆分模块。
- SQL 集中在 `store/` 模块；字段类型转换等规则实现采用 `(from, to)` 表驱动，防路径遗漏。

---

## 5. 前端交互

- 界面文案使用中文。
- 按钮/输入框报错时必须有明确的友好提示（如「保存失败，请重试」「标题不能为空」），禁止裸抛技术错误信息。
- UI 实现以 `doc/原型.html` 为契约：布局结构、信息层级、控件形态与原型一致，**禁止自行增删界面元素**；需要改原型时，先向用户提出变更，确认后再同步原型与代码。

---

## 6. 环境与安全

- **禁止**在代码中硬编码密钥、IP 或绝对路径；数据路径在运行时解析（exe 同目录）。
- 本项目**不使用 `.env`**（技术方案决策 D4）：无环境变量需求，授权码走 DPAPI 加密入库。
- 引入任何网络相关依赖前，必须先确认该项功能属于二期范围且符合红线第 1 条。

---

## 7. Git 与协作

- 阶段性任务完成后，提示用户提交代码，并生成中文 Commit Message。
- `.gitignore` 必须排除：`data\`、`node_modules\`、`dist\`、`src-tauri\target\`。
- **发布与打包铁律（2026-09-04 起，因便携版数据误删事故立规）**：
  - 用户以**便携版正式使用**（发布目录内 `data\` 是**生产数据**，含用户手动维护的记录），不是测试残留。
  - **严禁**删除/移动/覆盖任何已存在的 `data\`（含 `data\backups`）；严禁在含用户数据的目录执行清理。
  - 发布/重打包新版本：一律**新建独立版本目录**（如 `日历待办工具 v0.1.1\`）或仅替换 exe；若确需改动现有目录，先向用户确认 data 位置并由用户自行备份。
  - release exe 启动验证必须在**临时目录**进行（复制 exe 到临时目录运行），禁止在用户数据目录运行并清理。
  - 任何涉及删除 `data\` 或用户数据的操作，必须先经用户明确确认。

---

## 8. 文档更新触发条件（必选）

- **修改代码后**：文件结构或核心函数变化，必须同步更新 `PROJECT_MAP.md`。
- **修改业务规则后**：先更新 `doc/测试用例.md` 再改代码（用例先行），保持用例与规则同步。
- **UI 经确认变更后**：必须同步更新 `doc/原型.html`，历史版本不留档（变更记入 `CHANGELOG.md`）。
- **CHANGELOG 防重复**：每次只允许在文件末尾**追加本批次的新条目**；严禁把整个文件内容再次写入（曾因整文件重复追加导致内容嵌套翻倍，2026-09-04 已按 git 历史重建去重）。
- **结束对话前**：无论进度如何，必须更新 `TODO.md`（记录当前进度与下一步计划）。

---

## 9. 完成定义（DoD）

宣称「完成」前必须满足：

1. `doc/测试用例.md` 的**回归最小集**全部通过，且本次改动涉及模块的用例全部通过。
2. `cargo test` 与 `vitest` 全部通过。
3. `PROJECT_MAP.md`、`TODO.md` 已按第 8 条刷新。
4. 未做超出本次指令范围的"顺手重构"。
